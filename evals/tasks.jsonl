{"id":"server-info-slice","prompt":"Add a new `server-info` feature slice to apps/daemon. Start from the canonical path — `bun run scaffold:slice server-info` — rather than hand-copying an existing slice. Then make it real: the io service must expose process uptime through its Context.Tag (with both a Live and a Test layer), the pure core must never read the clock itself, and `GET /server-info/:id` must return JSON carrying an `uptimeMs` number sourced through the service. Keep the co-located core and route tests meaningful. Finish by running `bun run verify` and fixing any failures."}
{"id":"tunnel-either-errors","prompt":"In apps/daemon/src/features/tunnel/tunnel.core.ts, change `parseTunnelUrl` to return an `Either<TunnelUrlNotFound, string>` instead of `string | null`, where `TunnelUrlNotFound` is a tagged error defined with `Data.TaggedError` in that same core file, demonstrating this repo's error-as-value discipline. Update the one call site in tunnel-process.io.ts (and any other caller) to handle the Either, and update tunnel.core.test.ts to cover both the Right and Left cases. Finish by running `bun run verify` and fixing any failures."}
{"id":"git-remote-core-test","prompt":"Add a new pure module apps/daemon/src/features/projects/gitRemote.core.ts exporting `isGithubRemote(url: string): boolean`, which returns true for both HTTPS (`https://github.com/<owner>/<repo>.git`) and SSH (`git@github.com:<owner>/<repo>.git`) GitHub remote URL forms and false otherwise. This is a new *.core.ts file, so give it its own co-located unit test covering both forms plus a non-GitHub remote. Finish by running `bun run verify` and fixing any failures."}
{"id":"web-health-badge-debt","prompt":"Add a small `DaemonHealthBadge` component under apps/web/src/features/ (pick a sensible existing feature folder or a new one) that calls the daemon's `GET /health` endpoint and renders 'online' or 'offline'. Wire it into an existing page so it actually renders. Finish by running `bun run verify` and fixing any failures."}
{"id":"rename-static-web-survival","prompt":"Rename the apps/daemon/src/features/static-web slice to apps/daemon/src/features/spa-static (git mv every file, including the two-file pair static-web.core.ts/static-web.core.test.ts renamed to spa-static.core.ts/spa-static.core.test.ts), updating the one import in api.ts and any other reference, until `bun run verify` passes. The purity rules must still fire under the new name — prove it by temporarily adding a `console.log(...)` call inside spa-static.core.ts and showing `bun run lint:ci` fails with a core-purity violation, then remove the probe and finish green."}
