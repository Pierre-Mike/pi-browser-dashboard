node_modules/
dist/
build/
coverage/
.DS_Store
.vite/
.tanstack/
.turbo/
*.log
.env
.env.local
.env.*.local
bun.lockb

.claude/settings.local.json
.claude/worktrees/
.claude/plans/
apps/e2e/test-results/
apps/e2e/playwright-report/

# Local extensions/state under .pid/ stay local — EXCEPT this project's own
# committed dogfood pid-app (.pid/index.html), which the dashboard renders as a
# tab when pi-browser-dashboard is opened as a project (see the pid-apps feature
# in AGENTS.md). Local `.pid/extensions/` and everything else stays ignored.
.pid/*
!.pid/index.html
.fallow/

# Stale Lefthook stubs — hooks install to .git/hooks (see lefthook.yml, #136)
.githooks/
